apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: hp
spec:
  storageClassName: localstorage
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce